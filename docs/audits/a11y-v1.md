# Maestro 접근성 감사 v1 (Phase 22)

**감사 일자**: 2026-04-25
**범위**: Phase 22 ship 시점의 접근성 인프라 + 핵심 view 시범 적용

## 1. 인프라 현황

| 항목                          | 상태 | 위치                                                         |
| ----------------------------- | ---- | ------------------------------------------------------------ |
| 중앙 라벨 카탈로그            | ✅   | `Sources/MaestroCore/A11yLabels.swift`                       |
| ko/en 동시 정의 강제          | ✅   | `A11yLabelsTests.testAllA11yLabelsHaveBothLanguagesNonEmpty` |
| 키 네임스페이스 (`a11y.*`)    | ✅   | 테스트 강제                                                  |
| UI 키와 collision 차단        | ✅   | 테스트 강제                                                  |
| LocalizationKeys 와 동일 패턴 | ✅   | LocalizedKey struct 재사용                                   |

## 2. 카탈로그 정의 항목 (12개)

- **Folder**: addButton / removeButton / switchHint
- **Dispatch**: composer / sendButton / sendHint
- **Inbox**: panel / item
- **CommandPalette**: searchField / resultList
- **MenuBar**: trayIcon / summary

## 3. View 적용 현황

Phase 22 는 **카탈로그 정의 + 테스트 인프라 ship**. 실제 SwiftUI view 의
`.accessibilityLabel(...)` 적용은 진행 중:

| View               | 적용                                      | Phase |
| ------------------ | ----------------------------------------- | ----- |
| OnboardingView     | ⚠️ partial (Step indicator 만)            | 22    |
| PreferencesView    | ⚠️ TabView 표준 라벨                      | 22    |
| CommandPaletteView | 🔜 카탈로그 사용 마이그레이션 필요        | 23    |
| ControlTowerView   | 🔜 SidebarView / 토론 list 라벨 적용 필요 | 23    |
| InboxPanel         | ✅ accessibilityHint (Phase 12)           | 12    |
| ShellPanelView     | 🔜 탭 chip + close 버튼                   | 23    |

## 4. Dynamic Type / High Contrast / Keyboard

| 항목                  | 상태                                | 비고                                                   |
| --------------------- | ----------------------------------- | ------------------------------------------------------ |
| Dynamic Type          | ✅ 기본 SwiftUI `.font(.body)` 사용 | OnboardingView / PreferencesView 표준 SwiftUI controls |
| High Contrast         | ⚠️ 검증 안 됨                       | Phase 23 — `.accessibilityIgnoresInvertColors` 검토    |
| Keyboard 전체 사용    | ✅ Cmd+K / Cmd+1~9 / TabView focus  | NSWindow firstResponder chain 표준                     |
| VoiceOver 핵심 플로우 | ⚠️ 수동 검증 필요                   | Phase 23 — 실제 VO 사용                                |

## 5. Open Items (Phase 23+)

1. **모든 view 의 a11y 라벨 적용 마이그레이션** — 점진적
2. **VoiceOver 수동 검증** — 핵심 플로우 (폴더 추가, dispatch, 토론 시작) 음성 주행
3. **Dynamic Type 회귀 테스트** — Snapshot test 도입 시
4. **High Contrast 모드 시각 회귀** — Snapshot test
5. **VO 로 닫혀있는 sheet 자동 닫힘** — onboarding sheet 의 trap 방지
6. **단축키 도움말** — `?` 누르면 단축키 HUD (Phase 23 polish)

## 6. 검증 방법

```bash
# 단위 테스트 — 카탈로그 정합성
swift test --filter "A11yLabelsTests|LocalizationKeysTests"

# 수동 (VoiceOver)
# System Settings → Accessibility → VoiceOver 활성화 (Cmd+F5)
# Maestro 메인 윈도우에서 핵심 플로우 음성 주행
```

## 7. 결론

Phase 22 는 **"검증 가능한 인프라"** 단계 — 카탈로그 + 테스트로 향후 view 마이그레이션이
회귀 없이 가능. 실제 모든 view 의 라벨 적용 + VO 수동 검증은 Phase 23 에서.
