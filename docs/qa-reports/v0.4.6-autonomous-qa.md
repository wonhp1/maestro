# v0.4.6 자동 QA 리포트

**시작 시각**: 2026-04-26 (사용자 취침 중 자동 실행)
**테스터**: Claude Opus 4.7 (computer-use + Bash + 파일 시스템)
**대상 빌드**: v0.4.6 signed/notarized DMG
**리포트 파일**: 본 파일 — append-only 로그

## 검증 범위

v0.4.3 ~ v0.4.6 의 모든 사용자 노출 기능:

- [ ] **PATH augmentation** — Finder 더블클릭 시 claude CLI 발견
- [ ] **Vendor picker on add** — Claude/Aider 라디오 + ✓/✗ + 친절 카피
- [ ] **Auto-install** — 미설치 어댑터 "자동 설치" 버튼 → 진행 시트 → 설치
- [ ] **Control vendor 변경** — ⌘, → FolderSettingsSheet → adapter 변경 + 비-Claude warning
- [ ] **자식 ChatView dispatch sync** — control → 자식 dispatch 시 자식 폴더 채팅창에 메시지 표시
- [ ] **Multi-turn relay loop** — 자식 응답이 control 채팅에 `✓ **Name**: ...` follow-up 으로 도착
- [ ] **Inbox 라우팅** — 응답이 recipient (control) 보고함에 표시
- [ ] **RELAY 태그 strip** — 사용자 화면에 XML 태그 안 보임
- [ ] **Discussion 시작** — Sidebar "+ 새 토론" → 모달 → 시작 → list/detail mount
- [ ] **Diagnostics export** — 메뉴 → 진단 번들 → ZIP 저장
- [ ] **Crash review alert** — 직전 실행 크래시 있을 시 alert (이번 세션 트리거 X)
- [ ] **Friendly errors** — AdapterError 한국어 메시지 (npm install ... 안내)

## 진행 로그

(아래에 시나리오별 결과 append)

---
